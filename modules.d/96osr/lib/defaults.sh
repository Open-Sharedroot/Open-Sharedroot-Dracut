#!/bin/sh
# -*- mode: shell-script; indent-tabs-mode: nil; sh-basic-offset: 4; -*-
# ex: ts=8 sw=4 sts=4 et filetype=sh
#
# Library for the defaults

# default for the chroot mountpoint
DFLT_CHROOT_MOUNT="/cluster/shared/var/lib/osr/chroot"

# default for the chroot path
DFLT_CHROOT_PATH=$DFLT_CHROOT_MOUNT