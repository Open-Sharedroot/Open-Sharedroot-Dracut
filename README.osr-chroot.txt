IDEA:

Other rootfilesystems like GFS/GFS2/OCFS2 .. require a chroot environment where root dependent 
services (like the cluster infrastructure) have to be started and moved to the running root.

This module provides this functionality.