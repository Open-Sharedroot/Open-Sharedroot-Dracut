osr dracut module:

This module is a module for dracut (new initramfs) and provides open sharedroot functionality. This means
any shareable filesystem (so far only nfs) can be used as a filesystem single system image for multiple 
nodes.

For more information see www.open-sharedroot.org